Philosophy
    The elements are attached/linked at the end of each other in 
    the form of pointer in C. For scheme, they are immutable.
    They are in a fixed linear order controlled by the "client."
Use cases
    To cluster elements
    Recursion
    Apply
    map
    pipe-line
    reduce
Methods
    `list-ref`
    `index-of`
    `make-list`
    `append`
    `length`
    `car`
    `cdr`
    `empty?`
    `cons`
    `pair`

Layout
Implementation
Analysis